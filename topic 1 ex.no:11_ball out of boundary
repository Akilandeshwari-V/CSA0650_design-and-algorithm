def f(m,n,N,i,j):
    if i<0 or i>=m or j<0 or j>=n:
        return 1
    if N==0:
        return 0

    return (f(m,n,N-1,i+1,j)+
            f(m,n,N-1,i-1,j)+
            f(m,n,N-1,i,j+1)+
            f(m,n,N-1,i,j-1))

print(f(2,2,2,0,0))
